spring:
  main:
    web-application-type: none

tsanet:
  api:
    base-url: "http://localhost:8080"
  auth:
    username: "test-user"
    password: "test-password"
  storage:
    sqlite-path: "${java.io.tmpdir}/tsanet-test.db"
  cli:
    enabled: false
  webhook:
    enabled: false

crash:
  enabled: false
